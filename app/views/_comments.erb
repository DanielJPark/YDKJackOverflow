<div id="comments">
  <%@comments each do |comment|%>
    <p><%= comment.content %></p>
    <p><%= comment.user %></p>
    <% post_type = comment.post_type %>
    <% post_id = comment.post_id %>
  <% end %>
  <a href="/<%= post_type %>/<%= post_id %>/comment">Comment</a>
</div>
