<div>
  <% if selected_answer%>
    <div class="<%= 'selected_answer' if selected_answer %>">
      <p>This is the selected answer CHANGEME.</p>
    </div>
  <% end %>
  <p><%= answer.content %></p>
  <p><%= answer.score %></p>
  <%= erb :'_comments', layout: false, locals: { comments: answer.comments, post_type: "answer", post_id: answer.id } %>
</div>